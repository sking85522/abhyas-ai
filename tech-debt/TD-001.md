# TD-001: Extract Shared SDKs
**Issue**: Currently, shared utilities are in a monorepo shared folder.
**Resolution**: In the future, once we reach 10+ services, consider extracting `auth-sdk`, `logger-sdk`, etc., into their own private PyPI packages.
